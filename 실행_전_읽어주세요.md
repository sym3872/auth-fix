# SecureBlog API 실행 전 유의사항

## 가장 쉬운 실행 방법

1. ZIP 파일을 먼저 압축 해제한다.
2. 압축을 풀어 나온 **프로젝트 폴더 전체**를 VS Code로 연다.
3. `SecureBlog.code-workspace`를 더블클릭하면 폴더 전체를 정확하게 열 수 있다.
4. VS Code 위쪽 메뉴에서 **Terminal → New Terminal**을 누른다.
5. 열린 터미널에 `./run.sh`를 입력하고 Enter를 누른다.
6. `Swagger 문서 주소: http://localhost:8080/swagger-ui.html`가 보이면 서버가 정상 실행된 것이다.
7. 브라우저에서 위 주소를 열면 회원가입·로그인·게시글 API를 화면에서 바로 테스트할 수 있다.
8. 터미널 메뉴도 사용하려면 `1 회원가입 → 2 로그인 → 5 게시글 작성 → 3 목록 조회` 순으로 테스트한다.
9. 메뉴의 `0`은 메뉴만 닫는다. 서버를 완전히 끄려면 터미널에서 `Ctrl+C`를 누른다.

## 반드시 알아 둘 점

- Java 21을 사용한다. Spring Boot 4.1.0은 Java 17 이상이 필요하다.
- Java 파일 하나만 열어서 실행하면 non-project file 또는 cannot find symbol 오류가 날 수 있다. 반드시 build.gradle이 있는 폴더 전체를 열어야 한다.
- 첫 실행은 Gradle이 라이브러리를 받으므로 인터넷 연결이 필요하고 시간이 좀 걸릴 수 있다.
- Gradle을 따로 설치할 필요는 없다. 프로젝트에 gradlew가 포함되어 있다.
- 서버는 8080 포트를 사용한다. 다른 프로그램이 8080을 사용 중이면 그 프로그램을 종료해야 한다.
- macOS에서 run.sh의 권한 오류가 나면 프로젝트 터미널에서 chmod +x run.sh를 한 번 실행한다.
- H2 in-memory DB를 사용하므로 프로그램을 종료하면 회원과 게시글이 모두 사라진다. 
- 이메일은 test@example.com처럼 입력하고, 회원가입 비밀번호는 4글자 이상 입력한다.
